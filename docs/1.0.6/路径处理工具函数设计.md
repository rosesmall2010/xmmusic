# 路径处理工具函数设计文档

**设计日期**: 2025-12-10
**架构师**: Winston
**版本**: v1.0.6

---

## 一、设计目标

1. **跨平台兼容**：统一处理 Windows/macOS/Linux 路径格式
2. **路径规范化**：确保数据库中存储的路径格式一致
3. **性能优化**：减少路径字符串操作开销
4. **易于使用**：提供简洁的 API

---

## 二、核心函数设计

### 2.1 路径规范化函数

```typescript
/**
 * 规范化路径格式
 *
 * 规则：
 * 1. Windows: 统一使用正斜杠，去除末尾分隔符
 * 2. macOS/Linux: 去除末尾分隔符
 * 3. 处理多个连续分隔符
 *
 * @param path 原始路径
 * @param platform 平台类型（可选，默认使用 process.platform）
 * @returns 规范化后的路径
 *
 * @example
 * normalizePath('C:\\Music\\Rock\\', 'win32')  // => 'C:/Music/Rock'
 * normalizePath('/Users/name/Music/', 'darwin')  // => '/Users/name/Music'
 * normalizePath('/home/user/music//', 'linux')   // => '/home/user/music'
 */
export function normalizePath(
  path: string,
  platform: NodeJS.Platform = process.platform
): string {
  if (!path || typeof path !== 'string') {
    return path
  }

  // Windows: 统一使用正斜杠
  if (platform === 'win32') {
    path = path.replace(/\\/g, '/')
  }

  // 处理多个连续分隔符
  path = path.replace(/[/\\]+/g, '/')

  // 去除末尾分隔符（但保留根路径的单个斜杠）
  if (path.length > 1 && path.endsWith('/')) {
    path = path.slice(0, -1)
  }

  return path
}
```

### 2.2 完整路径构建函数

```typescript
/**
 * 构建完整文件路径
 *
 * @param dirPath 目录路径（已规范化）
 * @param fileName 文件名
 * @param platform 平台类型（可选，默认使用 process.platform）
 * @returns 完整文件路径
 *
 * @example
 * buildFullPath('C:/Music/Rock', 'song.mp3', 'win32')  // => 'C:\\Music\\Rock\\song.mp3'
 * buildFullPath('/Users/name/Music', 'song.mp3', 'darwin')  // => '/Users/name/Music/song.mp3'
 */
export function buildFullPath(
  dirPath: string,
  fileName: string,
  platform: NodeJS.Platform = process.platform
): string {
  if (!dirPath || !fileName) {
    throw new Error('dirPath and fileName are required')
  }

  // 确保 dirPath 已规范化
  const normalizedDir = normalizePath(dirPath, platform)

  // 根据平台选择分隔符
  const separator = platform === 'win32' ? '\\' : '/'

  return `${normalizedDir}${separator}${fileName}`
}
```

### 2.3 目录ID获取/创建函数

```typescript
/**
 * 获取或创建目录记录
 *
 * @param db 数据库实例
 * @param dirPath 目录路径
 * @param platform 平台类型（可选，默认使用 process.platform）
 * @returns 目录ID
 *
 * @example
 * const dirId = await getOrCreateMusicDir(db, 'C:/Music/Rock')
 */
export async function getOrCreateMusicDir(
  db: Database,
  dirPath: string,
  platform: NodeJS.Platform = process.platform
): Promise<number> {
  const normalizedPath = normalizePath(dirPath, platform)

  // 先查询是否存在
  const stmt = db.prepare('SELECT id FROM music_dir WHERE path = ?')
  const existing = stmt.get(normalizedPath) as { id: number } | undefined

  if (existing) {
    return existing.id
  }

  // 不存在则创建
  const insertStmt = db.prepare('INSERT INTO music_dir (path) VALUES (?)')
  const result = insertStmt.run(normalizedPath)

  return result.lastInsertRowid as number
}
```

### 2.4 批量目录ID获取/创建函数（性能优化）

```typescript
/**
 * 批量获取或创建目录记录（使用事务优化性能）
 *
 * @param db 数据库实例
 * @param dirPaths 目录路径数组
 * @param platform 平台类型（可选，默认使用 process.platform）
 * @returns 目录ID映射对象 { [path]: dirId }
 *
 * @example
 * const dirMap = await batchGetOrCreateMusicDir(db, ['C:/Music/Rock', 'C:/Music/Jazz'])
 * // => { 'C:/Music/Rock': 1, 'C:/Music/Jazz': 2 }
 */
export async function batchGetOrCreateMusicDir(
  db: Database,
  dirPaths: string[],
  platform: NodeJS.Platform = process.platform
): Promise<Record<string, number>> {
  if (!dirPaths || dirPaths.length === 0) {
    return {}
  }

  // 规范化所有路径
  const normalizedPaths = dirPaths.map(path => normalizePath(path, platform))
  const uniquePaths = [...new Set(normalizedPaths)]

  // 批量查询已存在的目录
  const placeholders = uniquePaths.map(() => '?').join(',')
  const selectStmt = db.prepare(`SELECT id, path FROM music_dir WHERE path IN (${placeholders})`)
  const existing = selectStmt.all(...uniquePaths) as Array<{ id: number; path: string }>

  const result: Record<string, number> = {}
  const existingMap = new Map<string, number>()

  // 构建已存在目录的映射
  for (const dir of existing) {
    existingMap.set(dir.path, dir.id)
    result[dir.path] = dir.id
  }

  // 找出需要创建的目录
  const toCreate = uniquePaths.filter(path => !existingMap.has(path))

  if (toCreate.length > 0) {
    // 使用事务批量插入
    const insertStmt = db.prepare('INSERT INTO music_dir (path) VALUES (?)')
    const insertMany = db.transaction((paths: string[]) => {
      const map: Record<string, number> = {}
      for (const path of paths) {
        const result = insertStmt.run(path)
        map[path] = result.lastInsertRowid as number
      }
      return map
    })

    const newMap = insertMany(toCreate)
    Object.assign(result, newMap)
  }

  return result
}
```

### 2.5 从完整路径解析目录和文件名

```typescript
/**
 * 从完整路径解析目录路径和文件名
 *
 * @param fullPath 完整文件路径
 * @param platform 平台类型（可选，默认使用 process.platform）
 * @returns { dirPath: string, fileName: string }
 *
 * @example
 * parsePath('C:/Music/Rock/song.mp3', 'win32')
 * // => { dirPath: 'C:/Music/Rock', fileName: 'song.mp3' }
 */
export function parsePath(
  fullPath: string,
  platform: NodeJS.Platform = process.platform
): { dirPath: string; fileName: string } {
  if (!fullPath) {
    throw new Error('fullPath is required')
  }

  // 规范化路径
  const normalizedPath = normalizePath(fullPath, platform)

  // 找到最后一个分隔符
  const lastSeparatorIndex = normalizedPath.lastIndexOf('/')

  if (lastSeparatorIndex === -1) {
    // 没有目录，只有文件名
    return { dirPath: '', fileName: normalizedPath }
  }

  const dirPath = normalizedPath.substring(0, lastSeparatorIndex)
  const fileName = normalizedPath.substring(lastSeparatorIndex + 1)

  return {
    dirPath: normalizePath(dirPath, platform),
    fileName
  }
}
```

### 2.6 从 all_music 记录构建完整路径

```typescript
/**
 * 从 all_music 记录构建完整文件路径
 *
 * @param db 数据库实例
 * @param musicRecord all_music 表记录（包含 dir_id 和 file_name）
 * @param platform 平台类型（可选，默认使用 process.platform）
 * @returns 完整文件路径
 *
 * @example
 * const music = db.prepare('SELECT * FROM all_music WHERE id = ?').get(1)
 * const fullPath = await buildPathFromMusicRecord(db, music)
 */
export async function buildPathFromMusicRecord(
  db: Database,
  musicRecord: { dir_id: number; file_name: string },
  platform: NodeJS.Platform = process.platform
): Promise<string> {
  // 查询目录路径
  const dirStmt = db.prepare('SELECT path FROM music_dir WHERE id = ?')
  const dir = dirStmt.get(musicRecord.dir_id) as { path: string } | undefined

  if (!dir) {
    throw new Error(`Directory with id ${musicRecord.dir_id} not found`)
  }

  return buildFullPath(dir.path, musicRecord.file_name, platform)
}
```

---

## 三、缓存策略

### 3.1 目录ID映射缓存

```typescript
/**
 * 目录ID映射缓存类
 * 用于缓存 path -> dir_id 映射，减少数据库查询
 */
export class MusicDirCache {
  private cache: Map<string, number> = new Map()
  private maxSize: number = 10000  // 最大缓存条目数

  /**
   * 获取目录ID（先从缓存查找）
   */
  async get(
    db: Database,
    dirPath: string,
    platform: NodeJS.Platform = process.platform
  ): Promise<number> {
    const normalizedPath = normalizePath(dirPath, platform)

    // 先从缓存查找
    if (this.cache.has(normalizedPath)) {
      return this.cache.get(normalizedPath)!
    }

    // 缓存未命中，查询数据库
    const dirId = await getOrCreateMusicDir(db, dirPath, platform)

    // 存入缓存
    this.setCache(normalizedPath, dirId)

    return dirId
  }

  /**
   * 批量获取目录ID
   */
  async batchGet(
    db: Database,
    dirPaths: string[],
    platform: NodeJS.Platform = process.platform
  ): Promise<Record<string, number>> {
    const normalizedPaths = dirPaths.map(path => normalizePath(path, platform))

    // 从缓存中查找已存在的
    const cached: Record<string, number> = {}
    const toFetch: string[] = []

    for (const path of normalizedPaths) {
      if (this.cache.has(path)) {
        cached[path] = this.cache.get(path)!
      } else {
        toFetch.push(path)
      }
    }

    // 批量查询数据库
    if (toFetch.length > 0) {
      const dbResult = await batchGetOrCreateMusicDir(db, toFetch, platform)

      // 存入缓存
      for (const [path, dirId] of Object.entries(dbResult)) {
        this.setCache(path, dirId)
      }

      Object.assign(cached, dbResult)
    }

    return cached
  }

  /**
   * 设置缓存（带大小限制）
   */
  private setCache(path: string, dirId: number): void {
    // 如果缓存已满，删除最旧的条目（FIFO）
    if (this.cache.size >= this.maxSize) {
      const firstKey = this.cache.keys().next().value
      this.cache.delete(firstKey)
    }

    this.cache.set(path, dirId)
  }

  /**
   * 清除缓存
   */
  clear(): void {
    this.cache.clear()
  }

  /**
   * 获取缓存大小
   */
  size(): number {
    return this.cache.size
  }
}

// 单例实例
export const musicDirCache = new MusicDirCache()
```

---

## 四、使用示例

### 4.1 文件扫描时使用

```typescript
import { normalizePath, getOrCreateMusicDir, buildFullPath } from './pathUtils'

async function scanMusicFile(db: Database, filePath: string) {
  // 1. 解析路径
  const { dirPath, fileName } = parsePath(filePath)

  // 2. 获取或创建目录ID
  const dirId = await getOrCreateMusicDir(db, dirPath)

  // 3. 插入音乐文件记录
  const stmt = db.prepare(`
    INSERT INTO all_music (dir_id, file_name, title, artist, ...)
    VALUES (?, ?, ?, ?, ...)
  `)

  stmt.run(dirId, fileName, title, artist, ...)
}
```

### 4.2 查询时构建完整路径

```typescript
import { buildPathFromMusicRecord } from './pathUtils'

async function getMusicWithFullPath(db: Database, musicId: number) {
  // 1. 查询音乐记录
  const music = db.prepare('SELECT * FROM all_music WHERE id = ?').get(musicId)

  // 2. 构建完整路径
  const fullPath = await buildPathFromMusicRecord(db, music)

  return {
    ...music,
    fullPath
  }
}
```

### 4.3 批量操作优化

```typescript
import { batchGetOrCreateMusicDir } from './pathUtils'

async function batchScanMusicFiles(db: Database, filePaths: string[]) {
  // 1. 提取所有目录路径
  const dirPaths = filePaths.map(path => parsePath(path).dirPath)
  const uniqueDirPaths = [...new Set(dirPaths)]

  // 2. 批量获取目录ID
  const dirIdMap = await batchGetOrCreateMusicDir(db, uniqueDirPaths)

  // 3. 批量插入音乐文件
  const insertStmt = db.prepare(`
    INSERT INTO all_music (dir_id, file_name, ...)
    VALUES (?, ?, ...)
  `)

  const insertMany = db.transaction((files) => {
    for (const file of files) {
      const { dirPath, fileName } = parsePath(file.path)
      const dirId = dirIdMap[normalizePath(dirPath)]
      insertStmt.run(dirId, fileName, ...)
    }
  })

  insertMany(filePaths.map(path => ({ path, ... })))
}
```

---

## 五、测试用例

### 5.1 路径规范化测试

```typescript
describe('normalizePath', () => {
  it('should normalize Windows paths', () => {
    expect(normalizePath('C:\\Music\\Rock\\', 'win32')).toBe('C:/Music/Rock')
    expect(normalizePath('C:/Music//Rock', 'win32')).toBe('C:/Music/Rock')
  })

  it('should normalize macOS/Linux paths', () => {
    expect(normalizePath('/Users/name/Music/', 'darwin')).toBe('/Users/name/Music')
    expect(normalizePath('/home/user/music//', 'linux')).toBe('/home/user/music')
  })

  it('should handle root paths', () => {
    expect(normalizePath('/', 'linux')).toBe('/')
    expect(normalizePath('C:/', 'win32')).toBe('C:/')
  })
})
```

### 5.2 完整路径构建测试

```typescript
describe('buildFullPath', () => {
  it('should build Windows paths', () => {
    expect(buildFullPath('C:/Music/Rock', 'song.mp3', 'win32')).toBe('C:\\Music\\Rock\\song.mp3')
  })

  it('should build macOS/Linux paths', () => {
    expect(buildFullPath('/Users/name/Music', 'song.mp3', 'darwin')).toBe('/Users/name/Music/song.mp3')
  })
})
```

---

## 六、性能考虑

1. **缓存策略**：使用 `MusicDirCache` 缓存目录ID映射，减少数据库查询
2. **批量操作**：使用事务批量插入目录和文件，提升性能
3. **路径规范化**：在存储前统一规范化，避免重复处理
4. **索引优化**：`music_dir.path` 字段建立索引，提升查询速度

---

## 七、总结

路径处理工具函数设计遵循以下原则：

1. **统一接口**：所有函数都接受 `platform` 参数，支持跨平台
2. **规范化优先**：所有路径在存储前都进行规范化
3. **性能优化**：提供批量操作和缓存机制
4. **易于使用**：提供简洁的 API，隐藏实现细节

这些函数将作为数据库操作的基础工具，确保路径处理的一致性和正确性。

---

**设计完成时间**: 2025-12-10
**文档版本**: v1.0
