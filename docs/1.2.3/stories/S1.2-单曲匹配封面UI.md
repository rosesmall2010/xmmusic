# S1.2 单曲匹配封面 UI

**Epic**: 1 — 匹配封面  
**优先级**: P0  
**状态**: done  
**依赖**: S1.1  
**估算**: 1～1.5 天  

---

## 用户故事

作为用户，我希望在列表右键与全屏播放都能打开候选列表来匹配/更换单曲封面与歌词：可预览后挑选，有有效封面/歌词时重新匹配需确认。

---

## 验收标准

1. **列表右键**（`SongList.vue`，与歌词匹配同开关）：  
   - 无有效封面 →「匹配封面」→ **弹出候选列表**（不设相似度下限；可预览 / 选本地图 / 取消），点选后 `apply-cover-candidate` / `apply-local-cover`。  
   - 有有效封面 →「重新匹配封面」→ confirm 后同样弹候选列表（`force: true`）。  
   - 「匹配/重新匹配歌词」同样弹候选列表（可预览歌词全文后挑选），不再因相似度过低直接跳过。  
2. **全屏播放页**（`NowPlayingView.vue`）：与「在线匹配歌词」同级；走**候选列表**（不设相似度下限；无论几条都弹窗预览；可另选本地图片；无在线结果也可打开弹窗仅用本地图），点选后 `apply-cover-candidate` / `apply-local-cover`。控制栏按钮 + 歌词区右键菜单均提供入口。  
3. 成功后刷新列表项；若为当前曲，播放栏/全屏/迷你封面立即更新。  
4. 非 MP3：提示已更新应用内封面、文件未修改。  
5. 匹配中防重复点击；失败展示可区分原因。  
6. i18n `zh.json` / `en.json`。  
7. `npm run build` + CHANGELOG。  

---

## 实现要点

- 右键与全屏均走候选弹窗（`LyricsMatchSelectModal` / `CoverMatchSelectModal`）；批量仍用自动 ≥50。  
- 「有效封面」与 S1.1 判定一致（路径存在且可读）。  

---

## 非目标

- 批量（S1.3）  

---

## 测试建议

- 右键低相似度仍弹出候选并可预览挑选。  
- 全屏选图与右键结果一致可写库。  
- 重新匹配确认框。  

---

### Review Findings

（2026-09-16 code review）

- [x] [Review][Decision] 右键「有效封面」判定 — 已加 `has-valid-cover` IPC，菜单打开前主进程校验
- [x] [Review][Patch] 切歌后 `applyCoverCandidate` 已成功仍派发 `music-metadata-updated` [NowPlayingView.vue:applyCoverSongId]
- [x] [Review][Patch] 无候选时 `closeCoverPick()` 清理状态 [NowPlayingView.vue:handleOnlineMatchCover]
- [x] [Product] 手动候选取消 ≥35 门槛；无论几条都弹窗预览，用户选择或取消 [coverMatchService + NowPlayingView]
- [x] [Product] 右键匹配歌词/封面改为候选弹窗，不再因相似度过低直接跳过 [SongList.vue]
- [x] [Review][Defer] 批量进行中右键仍可点单曲匹配（仅主进程抛错）— deferred，与歌词右键行为对齐
- [x] [Review][Patch] 拉取遮罩与 confirm 解耦；重新匹配确认前互斥；候选搜索持锁；无在线候选仅 linked_local；遮罩可取消 [SongList / NowPlaying / handlers]
- [x] [Review][Patch] finally 清占位绑 token；`link-local-lyrics` 替代 matchOne；手动匹配 busy 禁用批量 [SongList / lyricsMatch store]
