# 安装 electron / electron-builder 时使用国内镜像加速二进制下载
# 用 npm 原生支持的 electron_mirror 键（会作为 npm_config_electron_mirror 传给所有子进程，
# 不依赖 cwd），比之前用 NODE_OPTIONS --require 相对路径注入脚本更可靠——
# 那种写法在 postinstall 链里 cwd 变化的子进程（如 electron-winstaller）中会
# 因为找不到相对路径模块直接崩掉，导致 electron 真正的二进制没下载成功，
# 残留旧版本（这就是 Windows 上播放卡住排查到最后发现的真实原因）。
# npm 11+ 会有一条「Unknown project config」告警，无害，可忽略。
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
