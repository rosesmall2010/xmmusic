# 安装 electron / 运行脚本时注入国内镜像环境变量（见 scripts/electron-mirror-env.js）
# 不使用 electron_mirror 键，避免 npm 11+「Unknown project config」告警
# ${NODE_OPTIONS?} 保留已有 NODE_OPTIONS，避免覆盖 CI/本机的 --max-old-space-size 等
node-options="${NODE_OPTIONS?} --require=./scripts/electron-mirror-env.js"
